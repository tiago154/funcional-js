const mainMenu = require('./main');
const filterMenu = require('./filter');
const messagesMenu = require('./messages');
const operationsMenu = require('./operations');
const groupMenu = require('./group');

module.exports = {
    filterMenu,
    mainMenu,
    messagesMenu,
    operationsMenu,
    groupMenu
}
